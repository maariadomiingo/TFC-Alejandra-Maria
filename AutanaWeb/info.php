<?php
// Display PHP configuration
phpinfo();

// To set these values at runtime (if supported), use ini_set:
ini_set('curl.cainfo', 'C:\xampp\apache\bin\curl-ca-bundle.crt');
ini_set('openssl.cafile', 'C:\xampp\apache\bin\curl-ca-bundle.crt');
